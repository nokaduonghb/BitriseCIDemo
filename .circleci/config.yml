version: 2

defaults: &defaults
  working_directory: ~/code
  docker:
    - image: circleci/android:api-29
  environment:
    JVM_OPTS: -Xmx3200m

jobs:
  apply_ktlint:
    <<: *defaults
    steps:
      - checkout
      - restore_cache:
          key: jars-{{ checksum "build.gradle" }}-{{ checksum  "app/build.gradle" }}
      - run:
          name: Download Dependencies
          command: sudo chmod +x ./gradlew
      - save_cache:
          paths:
            - ~/.gradle
          key: jars-{{ checksum "build.gradle" }}-{{ checksum  "app/build.gradle" }}
      - run:
          with:
            ref: ${{ github.event.pull_request.head.ref }}
          name: Apply ktlint format
          command: ./gradlew ktlintFormat
      - run:
          name: Commit changes
          command: |
            git config --local user.email "duong.hoang@nokasoft.com"
            git config --local user.name "noka-bot"
            git commit -am "Apply ktlint Format"
            git push https://duonghb:${DANGER_GITHUB_API_TOKEN}@github.com/nokaduonghb/BitriseCIDemo.git
            exit 0
      - run:
          name: Run on fail status
          command: |
            echo "I am result of above failed job"
          when: on_fail

workflows:
  version: 2
  build_and_apply_ktlint:
    jobs:
      - apply_ktlint